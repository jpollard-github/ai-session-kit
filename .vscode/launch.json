{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Codex Session Kit",
      "type": "extensionHost",
      "request": "launch",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ]
    }
  ]
}
